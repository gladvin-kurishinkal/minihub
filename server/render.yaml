services:
  - type: web
    name: minihub-server
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: MONGO_URI
        sync: false
      - key: PORT
        value: 3001
      - key: FRONTEND_URL
        sync: false
